---
import BaseLayout from '../layouts/BaseLayout.astro';
import BattleCry from '../components/BattleCry.astro';
---

<BaseLayout>
  <script type="application/ld+json" is:inline>
    {JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'Organization',
      name: 'Journal Foundation',
      url: 'https://journalfoundation.org',
      publishingPrinciples: 'https://journalfoundation.org/privacy'
    })}
  </script>
  <div class="journalnet-banner">
    <p>
      <strong>JournalNet</strong>: Privacy-first communications network from Journal Foundation.
      <a href="/journalnet" class="journalnet-link">Meet the product →</a>
      <span aria-hidden="true" class="journalnet-separator">•</span>
      <a href="/docs" class="journalnet-link">Start building with JournalNet →</a>
    </p>
  </div>

  <!-- Hero -->
  <BattleCry>
    <div class="hero-quote">
      <p class="hero-lede">
        <strong>JournalNet is our flagship encrypted data layer.</strong>
        Keep intent and sensitive personal data inside custody-first vaults while still collaborating with people and AI you trust.
        <a href="/journalnet">Learn about JournalNet →</a>
      </p>
      <p>
        Disposable apps shouldn't translate your memories into behavioral nudges.
        We're designing protocols so the encrypted data layer stays sovereign—even when interfaces change.
      </p>
      <div class="hero-highlights" role="list">
        <article class="hero-highlight" role="listitem">
          <span class="hero-highlight-icon" aria-hidden="true">🔐</span>
          <div>
            <h4>Encrypted custody</h4>
            <p>Vault-first storage keeps keys with individuals and trusted circles.</p>
          </div>
        </article>
        <article class="hero-highlight" role="listitem">
          <span class="hero-highlight-icon" aria-hidden="true">🕵️‍♀️</span>
          <div>
            <h4>Metadata minimization</h4>
            <p>Blind relays and differential routing hide who talks to whom.</p>
          </div>
        </article>
        <article class="hero-highlight" role="listitem">
          <span class="hero-highlight-icon" aria-hidden="true">🤖</span>
          <div>
            <h4>AI without surveillance</h4>
            <p>Frontier models can assist without inheriting a shadow archive.</p>
          </div>
        </article>
      </div>
      <p class="hero-footer">
        Sensitive intent should never be a growth hack.
        We're standardizing selective access, accountable governance, and post-quantum resilience so people decide how intelligence meets their data.
      </p>
    </div>

    <div class="cta-group">
      <a href="/docs" class="btn btn--primary">
        🛠️ Build Apps
      </a>
      <a href="/learn" class="btn btn--secondary">
        📚 Learn Privacy
      </a>
      <a href="/governance#funding" class="btn btn--tertiary">
        💰 Support Us
      </a>
    </div>
  </BattleCry>

  <section class="snapshot-section">
    <div class="container">
      <div class="section-header">
        <h2>What You Can Do in Minutes</h2>
        <p class="subtitle">Six quick wins to explore, teach, and deploy JournalNet.</p>
      </div>
      <div class="snapshot-grid">
        <article class="snapshot-card card">
          <span class="snapshot-icon" aria-hidden="true">1</span>
          <h3>Skim the stack</h3>
          <p>Review the Vault → Cipherlot → Apps architecture at a glance.</p>
          <a class="snapshot-link" href="/stack">See the overview →</a>
        </article>
        <article class="snapshot-card card">
          <span class="snapshot-icon" aria-hidden="true">2</span>
          <h3>Try the quickstart</h3>
          <p>Spin up a local vault and share data capabilities in under ten minutes.</p>
          <a class="snapshot-link" href="/docs">Open the quickstart →</a>
        </article>
        <article class="snapshot-card card">
          <span class="snapshot-icon" aria-hidden="true">3</span>
          <h3>Map your data</h3>
          <p>Identify sensitive flows with our privacy worksheets for teams.</p>
          <a class="snapshot-link" href="/learn">Download worksheets →</a>
        </article>
        <article class="snapshot-card card">
          <span class="snapshot-icon" aria-hidden="true">4</span>
          <h3>Teach a workshop</h3>
          <p>Use ready-to-share decks that explain selective access and blind relays.</p>
          <a class="snapshot-link" href="/learn">Browse teaching kits →</a>
        </article>
        <article class="snapshot-card card">
          <span class="snapshot-icon" aria-hidden="true">5</span>
          <h3>Audit governance</h3>
          <p>Inspect transparency reports and funding flows for the network.</p>
          <a class="snapshot-link" href="/governance">Review governance →</a>
        </article>
        <article class="snapshot-card card">
          <span class="snapshot-icon" aria-hidden="true">6</span>
          <h3>Join the signal</h3>
          <p>Subscribe for changelog updates and testnet invitations.</p>
          <a class="snapshot-link" href="/press">Get updates →</a>
        </article>
      </div>
    </div>
  </section>
  
  <!-- Three Part Explainer -->
  <section class="explainer-section">
    <div class="container">
      <div class="section-header">
        <h2>Why This Matters</h2>
        <p class="subtitle">We're not building another app. We're hardening the data layer so intent and intelligence stay with the sovereign individual.</p>
      </div>
      
      <div class="explainer-grid">
        <div class="explainer-card card">
          <div class="explainer-number">1</div>
          <h3>The Problem</h3>
          <p>Sensitive personal data leaks into attention markets faster than we can contest it:</p>
          <ul>
            <li>Biometric, location, and relationship data get warehoused for resale</li>
            <li>Data brokers transport our personal data to police, advertisers, and political actors</li>
            <li>Platforms and AI systems remix our intent into behavioral nudges</li>
            <li>Frontier models learn from the archives we can't retract</li>
          </ul>
        </div>

        <div class="explainer-card card">
          <div class="explainer-number">2</div>
          <h3>Decentralization and Openness Are Not Enough</h3>
          <p>Software is becoming disposable. Without encrypted custodianship, the data layer still betrays us:</p>
          <ul>
            <li><strong>Metadata still exposed:</strong> Who talks to whom is visible</li>
            <li><strong>Federation centralizes:</strong> Your instance admin sees everything</li>
            <li><strong>Correlation attacks:</strong> Traffic patterns reveal identity</li>
            <li><strong>LLMs read public data:</strong> bespoke agents can mirror and manipulate you</li>
            <li><strong>Post-Quantum decryption:</strong> Download now, decrypt with quantum later</li>
          </ul>
        </div>

        <div class="explainer-card card">
          <div class="explainer-number">3</div>
          <h3>The Stack Difference</h3>
          <p>Infrastructure for sovereignty, not another social network:</p>
          <ul>
            <li>Encrypted by default—with local or mirrored custody</li>
            <li>Selective accessibility for the people and AI partners you choose</li>
            <li>Metadata minimization through blind relays</li>
            <li>Self-hostable without compromise</li>
            <li>Open-source data layer for bespoke, swappable apps</li>
          </ul>
        </div>
      </div>
    </div>
  </section>
  
  
  <!-- Stack Explainer -->
  <section class="stack-explainer">
    <div class="container">
      <div class="section-header">
        <h2>The Journal Protocol Stack</h2>
        <p class="subtitle">Three layers that enable apps without platforms</p>
      </div>

      <div class="stack-grid">
        <div class="stack-layer">
          <h3>📦 Layer 1: Vault</h3>
          <p>Encrypted storage you control</p>
        </div>
        <div class="stack-layer">
          <h3>🔒 Layer 2: Cipherlot</h3>
          <p>Capability-based sharing</p>
        </div>
        <div class="stack-layer">
          <h3>📱 Layer 3: Apps</h3>
          <p>Swappable interfaces</p>
        </div>
      </div>

      <div class="cta-group">
        <a href="/stack" class="btn btn--secondary">Learn How It Works →</a>
      </div>
    </div>
  </section>

  <!-- Get Started -->
  <section class="get-started-section">
    <div class="container">
      <div class="section-header">
        <h2>How to Engage</h2>
        <p class="subtitle">Three ways to participate</p>
      </div>

      <div class="explainer-grid">
        <div class="explainer-card card">
          <h3>🛠️ For Developers</h3>
          <p>Build intent-driven software atop encrypted personal vaults—no platform custody required.</p>
          <ul>
            <li>Read the specifications</li>
            <li>Try the quickstart guide</li>
            <li>Join technical discussions</li>
          </ul>
          <p class="card-link">
            <a href="/docs">Start building →</a>
          </p>
        </div>

        <div class="explainer-card card">
          <h3>📚 For Privacy Advocates</h3>
          <p>Teach communities how to protect sensitive PII while still using AI and shared tools.</p>
          <ul>
            <li>Learn privacy fundamentals</li>
            <li>Explore the protocol stack</li>
            <li>Share guides with your community</li>
          </ul>
          <p class="card-link">
            <a href="/learn">Visit learning center →</a>
          </p>
        </div>

        <div class="explainer-card card">
          <h3>💰 For Funders &amp; Advisors</h3>
          <p>Support open protocols that keep intent and data sovereign while enabling trusted collaboration.</p>
          <ul>
            <li>Review financial dashboard</li>
            <li>See governance model</li>
            <li>Contact for partnerships</li>
          </ul>
          <p class="card-link">
            <a href="/journalnet">Discover JournalNet's roadmap →</a>
          </p>
        </div>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
  /* Hero Quote */
  .journalnet-banner {
    background: rgba(45, 125, 255, 0.08);
    border: 1px solid rgba(45, 125, 255, 0.25);
    border-radius: var(--radius-lg);
    margin: var(--space-lg) auto var(--space-2xl);
    max-width: min(960px, 100%);
    padding: var(--space-xs) var(--space-lg);
    font-size: clamp(0.95rem, 2vw, 1.05rem);
    color: var(--color-text-primary);
  }

  .journalnet-banner p {
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-xs);
    align-items: center;
    justify-content: space-between;
    text-align: left;
  }

  .journalnet-banner p > * {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2xs);
  }

  .journalnet-banner strong {
    color: var(--color-accent-azure);
    font-weight: 700;
  }

  .journalnet-link {
    color: var(--color-accent-emerald);
    font-weight: 600;
    text-decoration: none;
  }

  .journalnet-link:hover,
  .journalnet-link:focus {
    text-decoration: underline;
  }

  .journalnet-separator {
    color: var(--color-border-stronger);
    font-weight: 700;
  }

  .hero-quote {
    background: linear-gradient(135deg, rgba(17, 24, 33, 0.85) 0%, rgba(5, 7, 10, 0.85) 100%);
    border: 1px solid var(--color-border-subtle);
    border-left: 4px solid var(--color-accent-amber);
    padding: var(--space-lg);
    margin: 0;
    max-width: min(900px, 100%);
    text-align: left;
    font-size: clamp(1.125rem, 2.5vw, 1.375rem);
    line-height: 1.7;
  }

  .hero-quote p {
    color: var(--color-text-primary);
    margin-bottom: var(--space-sm);
  }

  .hero-quote p:last-child {
    margin-bottom: 0;
  }

  .hero-quote strong {
    color: var(--color-accent-emerald);
    font-weight: 700;
  }

  .hero-lede {
    font-size: clamp(1.1rem, 2.5vw, 1.4rem);
    line-height: 1.6;
  }

  .hero-highlights {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: var(--space-md);
    padding: var(--space-md);
    margin: var(--space-md) 0;
    background: rgba(17, 24, 33, 0.6);
    border-radius: var(--radius-lg);
    border: 1px solid rgba(45, 125, 255, 0.25);
  }

  .hero-highlight {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: var(--space-sm);
    align-items: start;
  }

  .hero-highlight h4 {
    margin: 0 0 var(--space-2xs);
    color: var(--color-text-primary);
  }

  .hero-highlight p {
    margin: 0;
    color: var(--text-secondary);
    font-size: 0.95rem;
  }

  .hero-highlight-icon {
    font-size: 1.75rem;
  }

  .hero-footer {
    font-weight: 500;
  }

  /* CTAs */
  .cta-group {
    display: flex;
    gap: var(--space-sm);
    justify-content: center;
    flex-wrap: wrap;
    margin-top: var(--space-xl);
  }

  .cta-group .btn {
    min-width: min(220px, 100%);
  }

  /* Sections */
  section {
    padding: var(--space-3xl) 0;
    border-top: 1px solid var(--color-border-subtle);
  }
  
  .section-header {
    text-align: center;
    margin-bottom: var(--space-xl);
  }
  
  .section-header h2 {
    margin-bottom: var(--space-sm);
  }
  
  .subtitle {
    color: var(--text-muted);
    font-size: 1.125rem;
    max-width: 700px;
    margin: 0 auto;
  }
  
  /* Explainer Grid */
  .explainer-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--space-xl);
    margin-top: var(--space-xl);
  }

  .explainer-card {
    gap: var(--space-2xs);
    border-color: var(--color-border-subtle);
    transition: border-color var(--duration-base) var(--easing-standard),
      background-color var(--duration-base) var(--easing-standard);
  }

  .explainer-card:hover {
    border-color: rgba(45, 125, 255, 0.4);
    background: rgba(45, 125, 255, 0.08);
  }
  
  .explainer-number {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    background: rgba(45, 125, 255, 0.12);
    border: 1px solid rgba(45, 125, 255, 0.45);
    border-radius: 50%;
    font-weight: 700;
    font-size: 1.4rem;
    color: var(--color-accent-azure);
    margin-bottom: var(--space-sm);
  }
  
  .explainer-card h3 {
    margin-bottom: var(--space-sm);
    color: var(--color-text-primary);
  }
  
  .explainer-card ul {
    list-style: none;
    margin-top: var(--space-md);
  }
  
  .explainer-card li {
    padding-left: var(--space-md);
    position: relative;
    margin-bottom: var(--space-xs);
    color: var(--text-secondary);
  }
  
  .explainer-card li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: var(--electric-blue);
  }
  
  .explainer-card pre {
    background: var(--void);
    padding: var(--space-md);
    border-radius: 4px;
    overflow-x: auto;
    margin-top: var(--space-md);
    font-size: 0.875rem;
  }
  
  .explainer-card code {
    color: var(--encrypted-green);
    font-family: var(--font-mono);
  }
  
  .card-link {
    margin-top: var(--space-md);
  }
  
  /* Urgency Section */
  .urgency-section {
    background: linear-gradient(135deg, rgba(245, 158, 11, 0.1), rgba(37, 99, 235, 0.1));
    border-left: 4px solid var(--urgent-amber);
  }
  
  .urgency-content {
    max-width: 900px;
    margin: 0 auto;
  }
  
  .urgency-content h2 {
    color: var(--urgent-amber);
    margin-bottom: var(--space-lg);
  }
  
  .urgency-quote {
    font-size: 1.25rem;
    line-height: 1.8;
    margin-bottom: var(--space-lg);
    font-style: italic;
  }
  
  .urgency-quote p {
    margin-bottom: var(--space-md);
  }
  
  .urgency-conclusion {
    color: var(--text-primary);
    font-weight: 600;
    font-size: 1.25rem;
    margin-top: var(--space-lg);
  }

  /* Stack Explainer */
  .stack-explainer {
    background: rgba(45, 125, 255, 0.06);
  }

  /* Snapshot Section */
  .snapshot-section {
    background: linear-gradient(135deg, rgba(45, 125, 255, 0.05), rgba(16, 185, 129, 0.05));
  }

  .snapshot-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: var(--space-lg);
    margin-top: var(--space-xl);
  }

  .snapshot-card {
    position: relative;
    padding-top: var(--space-xl);
    border-color: rgba(45, 125, 255, 0.25);
    min-height: 220px;
  }

  .snapshot-card h3 {
    margin-bottom: var(--space-xs);
  }

  .snapshot-card p {
    color: var(--text-secondary);
  }

  .snapshot-icon {
    position: absolute;
    top: -22px;
    left: var(--space-md);
    width: 44px;
    height: 44px;
    border-radius: 50%;
    display: grid;
    place-items: center;
    background: var(--void);
    border: 2px solid rgba(45, 125, 255, 0.6);
    font-weight: 700;
    color: var(--color-accent-azure);
  }

  .snapshot-link {
    margin-top: auto;
    display: inline-flex;
    align-items: center;
    gap: var(--space-2xs);
    color: var(--color-accent-emerald);
    font-weight: 600;
    text-decoration: none;
  }

  .snapshot-link:hover,
  .snapshot-link:focus {
    text-decoration: underline;
  }

  .stack-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--space-lg);
    margin-top: var(--space-xl);
  }

  .stack-layer {
    background: var(--void-lighter);
    border: 1px solid rgba(45, 125, 255, 0.25);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
    text-align: left;
  }

  .stack-layer h3 {
    margin-bottom: var(--space-sm);
    color: var(--color-text-primary);
  }

  .stack-layer p {
    color: var(--text-secondary);
  }
  
  /* Responsive */
  @media (max-width: 768px) {
    .cta-group {
      flex-direction: column;
      align-items: stretch;
    }

    .cta {
      justify-content: center;
    }

    .journalnet-banner p {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--space-2xs);
    }
  }
</style>
