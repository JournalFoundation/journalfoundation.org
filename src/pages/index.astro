---
import BaseLayout from '../layouts/BaseLayout.astro';
import BattleCry from '../components/BattleCry.astro';
---

<BaseLayout>
  <script type="application/ld+json" is:inline>
    {JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'Organization',
      name: 'Journal Foundation',
      url: 'https://journalfoundation.org',
      publishingPrinciples: 'https://journalfoundation.org/privacy'
    })}
  </script>
  <div class="journalnet-banner" role="complementary">
    <span class="journalnet-eyebrow">Product spotlight</span>
    <p class="journalnet-banner-body">
      <strong>JournalNet</strong> keeps sensitive intent encrypted end-to-end.
      <a href="/journalnet" class="journalnet-link">Meet the product →</a>
      <span aria-hidden="true" class="journalnet-separator">•</span>
      <a href="/docs" class="journalnet-link">Start building with JournalNet →</a>
    </p>
  </div>

  <!-- Hero -->
  <BattleCry>
    <div class="hero-quote">
      <p class="hero-lede">
        <strong>JournalNet is our flagship encrypted data layer</strong>—protocols and tooling so people and organizations can steward sensitive personal data without ceding custody.
      </p>
      <div class="hero-grid">
        <div class="hero-copy">
          <ul class="hero-points">
            <li><strong>Keep intent sovereign:</strong> Move conversations, credentials, and insights without forfeiting custody to platforms.</li>
            <li><strong>Design for the sensitive:</strong> Treat biometric, location, and relationship trails as assets that require selective disclosure.</li>
            <li><strong>Bring your own intelligence:</strong> Run trusted AI and automation against encrypted vaults instead of public surveillance data.</li>
            <li><strong>Collaborate on your terms:</strong> Extend access to cooperatives, partners, or agents you explicitly authorize.</li>
          </ul>
        </div>
        <div class="hero-facts" aria-label="What JournalNet delivers">
          <div class="fact-card">
            <span class="fact-icon" aria-hidden="true">🔐</span>
            <div>
              <h4>Custody stays yours</h4>
              <p>Operate local or mirrored vaults with encrypted sync and capability-based sharing.</p>
            </div>
          </div>
          <div class="fact-card">
            <span class="fact-icon" aria-hidden="true">🧠</span>
            <div>
              <h4>Intelligence without leakage</h4>
              <p>Frontier LLMs connect through blind relays so they learn from context—not from your archives.</p>
            </div>
          </div>
        </div>
      </div>
      <p class="hero-footer">Software is dissolving into intent. If the data layer stays encrypted and sovereign, people decide when and how intelligence meets their memories.</p>
    </div>
    
    <div class="cta-group">
      <a href="/docs" class="btn btn--primary">
        🛠️ Build Apps
      </a>
      <a href="/learn" class="btn btn--secondary">
        📚 Learn Privacy
      </a>
      <a href="/governance#funding" class="btn btn--tertiary">
        💰 Support Us
      </a>
    </div>
  </BattleCry>
  
  <!-- Three Part Explainer -->
  <section class="explainer-section">
    <div class="container">
      <div class="section-header">
        <h2>Why This Matters</h2>
        <p class="subtitle">We're not building another app. We're hardening the data layer so intent and intelligence stay with the sovereign individual.</p>
      </div>
      
      <div class="explainer-grid">
        <div class="explainer-card card">
          <div class="explainer-number">1</div>
          <h3>The Problem</h3>
          <p>Sensitive personal data leaks into attention markets faster than we can contest it:</p>
          <ul>
            <li>Biometric, location, and relationship data get warehoused for resale</li>
            <li>Data brokers transport our personal data to police, advertisers, and political actors</li>
            <li>Platforms and AI systems remix our intent into behavioral nudges</li>
            <li>Frontier models learn from the archives we can't retract</li>
          </ul>
        </div>

        <div class="explainer-card card">
          <div class="explainer-number">2</div>
          <h3>Decentralization and Openness Are Not Enough</h3>
          <p>Software is becoming disposable. Without encrypted custodianship, the data layer still betrays us:</p>
          <ul>
            <li><strong>Metadata still exposed:</strong> Who talks to whom is visible</li>
            <li><strong>Federation centralizes:</strong> Your instance admin sees everything</li>
            <li><strong>Correlation attacks:</strong> Traffic patterns reveal identity</li>
            <li><strong>LLMs read public data:</strong> bespoke agents can mirror and manipulate you</li>
            <li><strong>Post-Quantum decryption:</strong> Download now, decrypt with quantum later</li>
          </ul>
        </div>

        <div class="explainer-card card">
          <div class="explainer-number">3</div>
          <h3>The Stack Difference</h3>
          <p>Infrastructure for sovereignty, not another social network:</p>
          <ul>
            <li>Encrypted by default—with local or mirrored custody</li>
            <li>Selective accessibility for the people and AI partners you choose</li>
            <li>Metadata minimization through blind relays</li>
            <li>Self-hostable without compromise</li>
            <li>Open-source data layer for bespoke, swappable apps</li>
          </ul>
        </div>
      </div>
    </div>
  </section>
  
  
  <!-- Stack Explainer -->
  <section class="stack-explainer">
    <div class="container">
      <div class="section-header">
        <h2>The Journal Protocol Stack</h2>
        <p class="subtitle">Three layers that enable apps without platforms</p>
      </div>

      <div class="stack-grid">
        <div class="stack-layer">
          <h3>📦 Layer 1: Vault</h3>
          <p>Encrypted storage you control</p>
        </div>
        <div class="stack-layer">
          <h3>🔒 Layer 2: Cipherlot</h3>
          <p>Capability-based sharing</p>
        </div>
        <div class="stack-layer">
          <h3>📱 Layer 3: Apps</h3>
          <p>Swappable interfaces</p>
        </div>
      </div>

      <div class="cta-group">
        <a href="/stack" class="btn btn--secondary">Learn How It Works →</a>
      </div>
    </div>
  </section>

  <!-- Get Started -->
  <section class="get-started-section">
    <div class="container">
      <div class="section-header">
        <h2>How to Engage</h2>
        <p class="subtitle">Three ways to participate</p>
      </div>

      <div class="explainer-grid">
        <div class="explainer-card card">
          <h3>🛠️ For Developers</h3>
          <p>Build intent-driven software atop encrypted personal vaults—no platform custody required.</p>
          <ul>
            <li>Read the specifications</li>
            <li>Try the quickstart guide</li>
            <li>Join technical discussions</li>
          </ul>
          <p class="card-link">
            <a href="/docs">Start building →</a>
          </p>
        </div>

        <div class="explainer-card card">
          <h3>📚 For Privacy Advocates</h3>
          <p>Teach communities how to protect sensitive PII while still using AI and shared tools.</p>
          <ul>
            <li>Learn privacy fundamentals</li>
            <li>Explore the protocol stack</li>
            <li>Share guides with your community</li>
          </ul>
          <p class="card-link">
            <a href="/learn">Visit learning center →</a>
          </p>
        </div>

        <div class="explainer-card card">
          <h3>💰 For Funders &amp; Advisors</h3>
          <p>Support open protocols that keep intent and data sovereign while enabling trusted collaboration.</p>
          <ul>
            <li>Review financial dashboard</li>
            <li>See governance model</li>
            <li>Contact for partnerships</li>
          </ul>
          <p class="card-link">
            <a href="/journalnet">Discover JournalNet's roadmap →</a>
          </p>
        </div>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
  /* Hero Quote */
  .journalnet-banner {
    background: rgba(45, 125, 255, 0.08);
    border: 1px solid rgba(45, 125, 255, 0.25);
    border-radius: var(--radius-lg);
    margin: 0 auto var(--space-xl);
    max-width: min(960px, 100%);
    padding: var(--space-sm) var(--space-lg);
    display: flex;
    flex-direction: column;
    gap: var(--space-2xs);
    color: var(--color-text-primary);
  }

  .journalnet-eyebrow {
    display: inline-flex;
    align-self: flex-start;
    padding: 0 var(--space-xs);
    border-radius: var(--radius-pill);
    background: rgba(45, 125, 255, 0.18);
    color: var(--color-accent-azure);
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .journalnet-banner-body {
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-xs);
    align-items: center;
  }

  .journalnet-banner strong {
    color: var(--color-accent-azure);
    font-weight: 700;
  }

  .journalnet-link {
    color: var(--color-accent-emerald);
    font-weight: 600;
    text-decoration: none;
  }

  .journalnet-link:hover,
  .journalnet-link:focus {
    text-decoration: underline;
  }

  .journalnet-separator {
    color: var(--color-border-stronger);
    font-weight: 700;
  }

  .hero-quote {
    background: linear-gradient(135deg, rgba(17, 24, 33, 0.85) 0%, rgba(5, 7, 10, 0.85) 100%);
    border: 1px solid var(--color-border-subtle);
    border-left: 4px solid var(--color-accent-amber);
    padding: var(--space-xl);
    margin: 0;
    max-width: min(1000px, 100%);
    text-align: left;
    line-height: 1.7;
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }

  .hero-lede {
    margin: 0;
    font-size: clamp(1.15rem, 2.5vw, 1.4rem);
    color: var(--color-text-primary);
  }

  .hero-quote strong {
    color: var(--color-accent-emerald);
    font-weight: 700;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: var(--space-xl);
  }

  .hero-points {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: var(--space-sm);
    font-size: clamp(1rem, 2vw, 1.1rem);
    color: var(--color-text-primary);
  }

  .hero-points li {
    padding-left: var(--space-lg);
    position: relative;
  }

  .hero-points li::before {
    content: '▹';
    position: absolute;
    left: 0;
    color: var(--color-accent-emerald);
  }

  .hero-facts {
    display: grid;
    gap: var(--space-md);
  }

  .fact-card {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: var(--space-sm);
    align-items: start;
    padding: var(--space-md);
    border-radius: var(--radius-lg);
    background: rgba(45, 125, 255, 0.12);
    border: 1px solid rgba(45, 125, 255, 0.3);
  }

  .fact-icon {
    font-size: 1.75rem;
    line-height: 1;
  }

  .fact-card h4 {
    margin: 0 0 var(--space-2xs);
    font-size: 1.05rem;
    color: var(--color-text-primary);
  }

  .fact-card p {
    margin: 0;
    color: var(--text-secondary);
    font-size: 0.95rem;
  }

  .hero-footer {
    margin: 0;
    font-size: clamp(1rem, 2vw, 1.125rem);
    color: var(--text-secondary);
  }

  /* CTAs */
  .cta-group {
    display: flex;
    gap: var(--space-sm);
    justify-content: center;
    flex-wrap: wrap;
    margin-top: var(--space-xl);
  }

  .cta-group .btn {
    min-width: min(220px, 100%);
  }

  /* Sections */
  section {
    padding: var(--space-3xl) 0;
    border-top: 1px solid var(--color-border-subtle);
  }
  
  .section-header {
    text-align: center;
    margin-bottom: var(--space-xl);
  }
  
  .section-header h2 {
    margin-bottom: var(--space-sm);
  }
  
  .subtitle {
    color: var(--text-muted);
    font-size: 1.125rem;
    max-width: 700px;
    margin: 0 auto;
  }
  
  /* Explainer Grid */
  .explainer-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--space-xl);
    margin-top: var(--space-xl);
  }

  .explainer-card {
    gap: var(--space-2xs);
    border-color: var(--color-border-subtle);
    transition: border-color var(--duration-base) var(--easing-standard),
      background-color var(--duration-base) var(--easing-standard);
  }

  .explainer-card:hover {
    border-color: rgba(45, 125, 255, 0.4);
    background: rgba(45, 125, 255, 0.08);
  }
  
  .explainer-number {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    background: rgba(45, 125, 255, 0.12);
    border: 1px solid rgba(45, 125, 255, 0.45);
    border-radius: 50%;
    font-weight: 700;
    font-size: 1.4rem;
    color: var(--color-accent-azure);
    margin-bottom: var(--space-sm);
  }
  
  .explainer-card h3 {
    margin-bottom: var(--space-sm);
    color: var(--color-text-primary);
  }
  
  .explainer-card ul {
    list-style: none;
    margin-top: var(--space-md);
  }
  
  .explainer-card li {
    padding-left: var(--space-md);
    position: relative;
    margin-bottom: var(--space-xs);
    color: var(--text-secondary);
  }
  
  .explainer-card li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: var(--electric-blue);
  }
  
  .explainer-card pre {
    background: var(--void);
    padding: var(--space-md);
    border-radius: 4px;
    overflow-x: auto;
    margin-top: var(--space-md);
    font-size: 0.875rem;
  }
  
  .explainer-card code {
    color: var(--encrypted-green);
    font-family: var(--font-mono);
  }
  
  .card-link {
    margin-top: var(--space-md);
  }
  
  /* Urgency Section */
  .urgency-section {
    background: linear-gradient(135deg, rgba(245, 158, 11, 0.1), rgba(37, 99, 235, 0.1));
    border-left: 4px solid var(--urgent-amber);
  }
  
  .urgency-content {
    max-width: 900px;
    margin: 0 auto;
  }
  
  .urgency-content h2 {
    color: var(--urgent-amber);
    margin-bottom: var(--space-lg);
  }
  
  .urgency-quote {
    font-size: 1.25rem;
    line-height: 1.8;
    margin-bottom: var(--space-lg);
    font-style: italic;
  }
  
  .urgency-quote p {
    margin-bottom: var(--space-md);
  }
  
  .urgency-conclusion {
    color: var(--text-primary);
    font-weight: 600;
    font-size: 1.25rem;
    margin-top: var(--space-lg);
  }
  
  /* Stack Explainer */
  .stack-explainer {
    background: rgba(45, 125, 255, 0.06);
  }

  .stack-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--space-lg);
    margin-top: var(--space-xl);
  }

  .stack-layer {
    background: var(--void-lighter);
    border: 1px solid rgba(45, 125, 255, 0.25);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
    text-align: left;
  }

  .stack-layer h3 {
    margin-bottom: var(--space-sm);
    color: var(--color-text-primary);
  }

  .stack-layer p {
    color: var(--text-secondary);
  }
  
  /* Responsive */
  @media (max-width: 768px) {
    .cta-group {
      flex-direction: column;
      align-items: stretch;
    }

    .cta {
      justify-content: center;
    }

    .journalnet-banner {
      align-items: flex-start;
    }
  }
</style>
